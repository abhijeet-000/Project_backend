/** @type {import('tailwindcss').Config} */
module.exports = {
content: ['./src/**/*.{js,jsx,ts,tsx}', './public/index.html'], // Specify paths to files
theme: {
extend: {}, // Customize or add new values if needed
},
  plugins: [], // Add plugins as needed
  }
  
